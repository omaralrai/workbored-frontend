const EmployerDashboard = () => {
  return (
    <div className="container page-placeholder">
      <h1>Employer Dashboard</h1>
      <p className="muted">Employer dashboard page — built in a later layer.</p>
    </div>
  );
};

export default EmployerDashboard;
