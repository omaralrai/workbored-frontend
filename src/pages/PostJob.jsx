const PostJob = () => {
  return (
    <div className="container page-placeholder">
      <h1>Post a Job</h1>
      <p className="muted">Job posting flow — built in a later layer.</p>
    </div>
  );
};

export default PostJob;
